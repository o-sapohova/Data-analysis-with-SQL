# Data-analysis-with-SQL
SQL query for analyzing the dynamics of account creation, user activity based on email metrics (sends, opens, clicks), and assessing behavior across categories such as sending interval, account verification, and subscription status.
The query leverages CTEs, JOINs, UNION ALL, aggregations, window functions, and ranking.
The result is the top 10 countries by account creation or open emails over time.
